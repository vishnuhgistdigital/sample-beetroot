apply plugin: 'cpp'

model {
  components {
    hello(NativeExecutableSpec) {
      sources {
        cpp {
          source {
            srcDir "src/hello/cpp"
          }
        }
      }
    }
  }
}
